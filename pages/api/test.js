
// import { ObjectId } from "mongodb";
// import client from "../../src/utils/dbConnection";

// export default async function handler(req, res) {

//     if (req.method === "GET") {
//       // const id = req.query.id
//       // console.log(id);
//       const videoCollection = await client.db("video-application").collection("videos");
//       const video = await videoCollection.find({});
//       res.status(200).json({ status: true, data: video.toArray() });
//     }
 
// }